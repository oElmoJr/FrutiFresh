import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#19381F",
    },
    secondary: {
      main: "#EAEDD4",
    },
    background: {
      default: "#EAEDD4",
      paper: "#fff",
    },
  },
});

export default theme;
